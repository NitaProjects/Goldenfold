@model IEnumerable<HospitalApi.Habitaciones>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Edificio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Planta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Habitacion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Edificio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Planta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Habitacion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID_Habitacion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID_Habitacion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID_Habitacion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
